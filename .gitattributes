* text=auto
*.sh text eol=lf

# Stage 5.5 preregistration artifacts are pinned on LF bytes so the byte-level
# hashes in the master preregistration survive checkout on any platform.
configs/experiments/stage5/stage5_5_*.json text eol=lf
